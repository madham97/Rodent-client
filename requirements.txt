# Monitoring Pipeline - Requirements

Flask==3.0.0
Werkzeug==3.0.1
pyserial==3.5
Pillow>=10.0.0

# Optional: picamera2 library for advanced camera control (requires libcamera-dev)
# picamera2>=0.3.14

# Thermal camera (Waveshare MI48 80×62) — install via system packages + pip:
#   sudo apt-get install -y python3-smbus python3-spidev python3-gpiozero \
#       python3-numpy python3-opencv python3-setuptools python3-pip
#   sudo pip3 install --break-system-packages crcmod cmapy imutils pysenxor
# pysenxor is not on PyPI; source zip from Waveshare:
#   https://files.waveshare.com/wiki/Thermal_Camera_Module/Thermal_Camera_Hat.zip
# See docs/thermal-camera-setup.md for full instructions.
